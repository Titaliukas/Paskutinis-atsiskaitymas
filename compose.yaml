name: paskutinis-atsiskaitymas

services:
  game-server:
    image: paskutinis-atsiskaitymas-server:local
    build: .
    ports:
      - "${SERVER_PORT:-8080}:8080"
    environment:
      SERVER_PORT: "8080"
    init: true
    stop_grace_period: 10s
